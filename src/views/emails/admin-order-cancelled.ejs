<% include("_helpers") %>
<%- include("_layout", {
  title: "Porudžbina otkazana od strane kupca",
  eyebrow: "Admin · Porudžbina",
  heading: "Porudžbina otkazana od strane kupca",
  preheader: "Kupac je otkazao porudžbinu",
  body: `
    ${infoTable(
      infoRow("Klijent", order.korisnik?.ime || "-") +
      infoRow("Email", order.korisnik?.email || "-") +
      infoRow("Ukupno", order.ukupnaCena || "-") +
      (order.razlogOtkazivanja ? infoRow("Razlog", order.razlogOtkazivanja) : "")
    )}
    ${ctaButton(adminUrl, "Pregledaj u admin panelu")}
  `
}) %>
