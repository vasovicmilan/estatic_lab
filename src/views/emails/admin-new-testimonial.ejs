<%- include("_layout", {
  title: "Novi testimonijal",
  eyebrow: "Admin · Testimonijal",
  heading: "Novi testimonijal čeka odobrenje",
  preheader: "Novi testimonijal čeka odobrenje",
  body: `
    ${infoTable(
      infoRow("Ime", testimonial.name || "-") +
      infoRow("Ocena", `${"★".repeat(testimonial.rating || 0)}${"☆".repeat(5 - (testimonial.rating || 0))}`)
    )}
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0;">Pregledajte i odobrite u admin panelu pod Testimonijali.</p>
  `
}) %>