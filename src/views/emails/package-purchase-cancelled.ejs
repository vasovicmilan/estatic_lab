<% include("_helpers") %>
<%- include("_layout", {
  title: "Paket otkazan",
  eyebrow: "Paket",
  heading: `Paket otkazan${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}`,
  preheader: "Vaš paket tretmana je otkazan",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">
      Ako mislite da je ovo greška, javite nam se na <a href="mailto:${SUPPORT_EMAIL}" style="color:#7a5c3e;">${SUPPORT_EMAIL}</a>.
    </p>
    ${infoTable(infoRow("Paket", purchase.paket || "-"))}
  `
}) %>
