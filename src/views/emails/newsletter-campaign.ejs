<%- include("_layout", {
  title: campaign.subject || "Novosti",
  eyebrow: "Newsletter",
  preheader: campaign.subject || "",
  body: `
    ${campaign.body || ""}
    <p style="font-size:12px; color:#aca59d; line-height:1.6; margin:28px 0 0; text-align:center;">
      Ne želite više da primate ove poruke? <a href="${unsubscribeUrl}" style="color:#7a5c3e;">Odjavite se ovde</a>.
    </p>
  `
}) %>
