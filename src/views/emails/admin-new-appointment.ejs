<%- include("_layout", {
  title: "Novi termin zakazan",
  eyebrow: "Admin · Termin",
  heading: "Novi termin zakazan",
  preheader: "Novi termin čeka pregled",
  body: `
    ${infoTable(
      infoRow("Klijent", appointment.korisnik?.ime || "-") +
      infoRow("Email", appointment.korisnik?.email || "-") +
      infoRow("Telefon", appointment.korisnik?.telefon || "-") +
      infoRow("Usluga", appointment.usluga?.naziv || "-") +
      infoRow("Termin", appointment.termin?.pocetak || "-") +
      infoRow("Status", badge(appointment.status || "-", statusTone(appointment.status)))
    )}
    ${ctaButton(adminUrl, "Pregledaj u admin panelu")}
  `
}) %>