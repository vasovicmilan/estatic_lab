<%- include("_layout", {
  title: "Termin otkazan",
  eyebrow: "Termin",
  heading: `Termin otkazan${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}`,
  preheader: "Vaš termin je otkazan",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">Vaš termin je otkazan. Slobodno zakažite novi termin kad god vam odgovara.</p>
    ${infoTable(
      infoRow("Usluga", appointment.usluga?.naziv || "-") +
      infoRow("Termin", appointment.termin?.pocetak || "-") +
      (appointment.razlogOtkazivanja ? infoRow("Razlog", appointment.razlogOtkazivanja) : "")
    )}
  `
}) %>