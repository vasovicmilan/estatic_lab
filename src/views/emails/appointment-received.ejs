<%- include("_layout", {
  title: "Zahtev za termin primljen",
  eyebrow: "Termin",
  heading: `Zahtev primljen${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}!`,
  preheader: "Vaš zahtev za termin je primljen i čeka potvrdu",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">Primili smo vaš zahtev za termin. Javićemo vam se čim ga terapeut potvrdi.</p>
    ${infoTable(
      infoRow("Usluga", appointment.usluga?.naziv || "-") +
      infoRow("Termin", appointment.termin?.pocetak || "-") +
      infoRow("Cena", appointment.konacnaCena || "-")
    )}
    ${ctaButton(manageUrl, "Pregledaj moje termine")}
  `
}) %>