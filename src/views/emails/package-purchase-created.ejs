<%- include("_layout", {
  title: "Vaš paket je aktiviran",
  eyebrow: "Paket",
  heading: `Vaš paket je aktiviran${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}!`,
  preheader: "Vaš paket tretmana je spreman za korišćenje",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">Termine u okviru paketa zakazujete kao i obično - samo javite terapeutu da plaćate iz paketa.</p>
    ${infoTable(
      infoRow("Paket", purchase.paket || "-") +
      infoRow("Plaćeno", purchase.placeno != null ? purchase.placeno + " RSD" : "-") +
      (purchase.istice ? infoRow("Važi do", purchase.istice) : "")
    )}
    ${ctaButton(manageUrl, "Zakaži termin")}
  `
}) %>