<%- include("_layout", {
  title: "Termin potvrđen",
  eyebrow: "Termin",
  heading: `Termin potvrđen${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}!`,
  preheader: "Vaš termin je potvrđen",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">Vidimo se uskoro!</p>
    ${infoTable(
      infoRow("Usluga", appointment.usluga?.naziv || "-") +
      infoRow("Termin", appointment.termin?.pocetak || "-") +
      infoRow("Terapeut", appointment.terapeut || "Biće dodeljen") +
      infoRow("Cena", appointment.konacnaCena || "-")
    )}
  `
}) %>