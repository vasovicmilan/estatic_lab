<% include("_helpers") %>
<%- include("_layout", {
  title: "Novi termin dodeljen",
  eyebrow: "Termin",
  heading: `Novi termin dodeljen${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}`,
  preheader: "Dodeljen vam je novi termin",
  body: `
    <p style="font-size:14px; color:#5b564f; line-height:1.6; margin:0 0 20px;">Dodeljen vam je termin koji je prethodno bio kod drugog terapeuta ili nedodeljen.</p>
    ${infoTable(
      infoRow("Klijent", appointment.korisnik?.ime || "-") +
      infoRow("Usluga", appointment.usluga?.naziv || "-") +
      infoRow("Termin", appointment.termin?.pocetak || "-")
    )}
    ${ctaButton(manageUrl, "Pregledaj termin")}
  `
}) %>
