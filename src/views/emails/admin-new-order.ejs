<%- include("_layout", {
  title: "Nova porudžbina",
  preheader: "Nova porudžbina čeka obradu",
  body: `
    <h1 style="font-size:20px; color:#333; margin:0 0 16px;">Nova porudžbina</h1>
    <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color:#f9f7f5; border-radius:6px; margin:0 0 16px;">
      <tr><td style="padding:16px; font-size:14px; color:#333;">
        <strong>Klijent:</strong> ${order.korisnik?.ime || "-"}<br>
        <strong>Email:</strong> ${order.korisnik?.email || "-"}<br>
        <strong>Telefon:</strong> ${order.korisnik?.telefon || "-"}<br>
        <strong>Stavke:</strong><br>
        ${order.stavke.map(function(item) {
          return `&nbsp;&nbsp;${item.naziv} (${item.varijanta}) &times; ${item.kolicina}<br>`;
        }).join("")}
        <strong>Ukupno:</strong> ${order.ukupnaCena}<br>
        <strong>Status:</strong> ${order.status}
      </td></tr>
    </table>
    <p style="text-align:center; margin:32px 0;">
      <a href="${adminUrl}" style="background-color:#7a5c3e; color:#ffffff; padding:12px 28px; border-radius:4px; text-decoration:none; font-size:14px; font-weight:bold; display:inline-block;">Pregledaj u admin panelu</a>
    </p>
  `
}) %>