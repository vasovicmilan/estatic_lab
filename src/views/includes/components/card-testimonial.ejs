<div class="card h-100 shadow-sm">
  <div class="card-body">
    <div class="d-flex align-items-center gap-2 mb-2">
      <span class="text-warning star-rating"><%= testimonial.ocenaZvezdice %></span>
      <span class="star-rating-count">/<% if (typeof testimonial.ocena !== 'undefined') { %><%= Number(testimonial.ocena).toFixed(1).replace('.', ',') %><% } %></span>
    </div>
    <p class="fst-italic">"<%= testimonial.komentar %>"</p>
    <div class="d-flex align-items-center gap-2 mt-3">
      <% if (testimonial.slika && testimonial.slika.url) { %>
        <img src="<%= testimonial.slika.url %>" class="rounded-circle avatar-sm" alt="<%= testimonial.ime %>" loading="lazy" decoding="async">
      <% } %>
      <div>
        <div class="fw-semibold small"><%= testimonial.ime %></div>
        <% if (testimonial.usluga) { %><div class="text-muted small"><%= testimonial.usluga %></div><% } %>
        <% if (testimonial.paket) { %><div class="text-muted small"><%= testimonial.paket %></div><% } %>
      </div>
    </div>
  </div>
</div>