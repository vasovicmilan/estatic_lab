<%- include("_layout", {
  title: "Paket otkazan",
  preheader: "Vaš paket tretmana je otkazan",
  body: `
    <h1 style="font-size:20px; color:#333; margin:0 0 16px;">Paket otkazan${typeof firstName !== 'undefined' && firstName ? ', ' + firstName : ''}</h1>
    <p style="font-size:14px; color:#555; line-height:1.6; margin:0 0 16px;">
      Vaš paket tretmana je otkazan. Ako mislite da je ovo greška, kontaktirajte nas na
      <a href="mailto:${SUPPORT_EMAIL}" style="color:#7a5c3e;">${SUPPORT_EMAIL}</a>.
    </p>
    <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color:#f9f7f5; border-radius:6px; margin:0 0 16px;">
      <tr><td style="padding:16px; font-size:14px; color:#333;">
        <strong>Paket:</strong> ${purchase.paket || "-"}
      </td></tr>
    </table>
  `
}) %>